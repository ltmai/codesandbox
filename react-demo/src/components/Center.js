import React from "react";

/**
 * Minimal React component that centers all its children
 * @param {*} props
 */
const Center = (props) => <div align="center">{props.children}</div>;

export default Center;
